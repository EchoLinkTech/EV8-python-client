# EchoLink API Client Configuration
# Create a copy of this file named ".env" or "configs/.env" and fill in your values

# Required: Your Ethereum private key (without 0x prefix)
# This is used to sign authentication messages to the API
PRIVATE_KEY=your_ethereum_private_key_here

# Optional: API endpoint URL
# Defaults to http://localhost:5000 if not specified
EV8_API_URL=http://localhost:5000

# Optional: Configure logging level
# Possible values: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# Optional: Transaction timeout in seconds
# Default is 300 seconds (5 minutes)
# TX_TIMEOUT=300
